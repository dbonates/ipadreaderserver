 <%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %> 

 	<table>
		<tr>
			<th class = 'control-label'><%= f.label :email %></th>
			<td><%= f.text_field :email, :class => 'text_field' %></td>
		</tr>
		<tr>
			<th class = 'control-label'><%= f.label :password %></th>
			<td><%= f.password_field :password, :class => 'text_field' %></td>
		</tr>
		<tr>
			<th class = 'control-label'><%= f.label :admin %></th>
			<td><%= f.check_box :admin, :class => 'text_field' %></td>
		</tr>
		
		
	</table>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>